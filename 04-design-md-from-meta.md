# 프롬프트: 코드베이스에서 DESIGN.md 추출 (유실 최소화)

> 아래 전문을 Claude에게 그대로 전달한다.
> 목적: 이미 구현된 디자인(토큰·컴포넌트·화면·인터랙션)을 빠짐없이 문서화한 `DESIGN.md` 생성.

---

이 프로젝트의 디자인 시스템을 역추출해서 `DESIGN.md` 파일로 만들어줘.
이 문서 하나만 보고 **코드를 전혀 보지 않은 디자이너/개발자가 동일한 UI를 재구현할 수 있는 수준**이 목표야.

## 1. 원본 (전부 직접 읽을 것 — 요약본이나 기억에 의존 금지)

- `app/globals.css` — 디자인 토큰의 단일 원천(색·타이포·간격·radius·그림자·모션)
- `app/layout.tsx` — 루트 레이아웃, Pretendard 로컬 폰트 로딩
- `app/login/page.tsx`, `app/(app)/layout.tsx`, `app/(app)/page.tsx`, `app/(app)/mypage/page.tsx` — 화면별 레이아웃
- `components/IconRail.tsx`, `PostList.tsx`, `Editor.tsx`, `EmptyState.tsx`, `icons.tsx` — 컴포넌트 구현
- `lib/data.ts`, `lib/store.tsx`, `lib/auth.ts` — 상태·데이터가 UI에 노출되는 방식(시간 표기, 시드 데이터, 빈 상태 조건)
- `02-prd.md` — 기능 맥락 (디자인 의도 해석용)
- `03-reference-design.png` — 참조 디자인 이미지 (코드와 다르면 **코드가 정답**, 단 차이점은 문서에 별도 표기)
- `README.md` — 화면 흐름도, 디자인 출처

## 2. 유실 방지 규칙 (가장 중요)

1. **값은 리터럴 그대로 기록한다.** hex 코드, px/rem, font-weight 숫자, ms, cubic-bezier 등을 원문 그대로 옮긴다. "연한 회색", "적당한 여백" 같은 서술로 대체 금지.
2. **토큰은 전수 기록한다.** `globals.css`의 모든 CSS 커스텀 프로퍼티(`--gray-*`, `--blue-*`, `--text-*`, `--surface-*`, `--border-*`, `--accent*` 등)를 하나도 빠뜨리지 말고, 프리미티브 → 시맨틱 참조 관계(`--text-primary: var(--gray-1000)`)도 함께 적는다.
3. **토큰 밖의 하드코딩 값도 수집한다.** 컴포넌트/페이지의 인라인 스타일·클래스에 박힌 수치(레일 너비, 목록 패널 너비, 아바타 크기, 이모지 크기 등)를 찾아서 기록한다.
4. **모든 상태를 명세한다.** 각 컴포넌트마다 default / hover / active / focus / disabled / 빈 상태 / 에러·확인(삭제 확인 팝오버 등)을 다룬다. 코드에 없는 상태는 지어내지 말고 "미구현"으로 표기한다.
5. **모르면 "미정"이라고 쓴다.** 코드에서 확인되지 않는 내용을 그럴듯하게 추측해서 채우지 않는다.
6. **한국어 UI 문자열도 원문 그대로 기록한다.** 버튼 라벨, placeholder, 빈 상태 문구, 확인 문구 등.

## 3. DESIGN.md 필수 목차

1. **개요** — 제품명(Nook), 디자인 컨셉, 참조 디자인 출처
2. **디자인 토큰**
   - 색상: 프리미티브 팔레트 전체 + 시맨틱 토큰(텍스트/서피스/보더/액센트/상태색/타일색) 매핑 표
   - 타이포그래피: 폰트(Pretendard, 로딩 방식 포함), 크기·굵기·행간·자간 스케일
   - 간격·크기: spacing 스케일, radius, 그림자, z-index(있다면)
   - 모션: transition 속성·duration·easing
3. **레이아웃** — 화면별(로그인 / 업무 페이지 3분할 / 글 상세 / 마이 페이지) 구조와 고정 치수, 스크롤 영역
4. **컴포넌트 명세** — IconRail, PostList(검색·항목·시간 표기 규칙 포함), Editor(이모지·커버·제목·본문·자동 저장 표시), EmptyState, 버튼/입력류. 각각 구조·치수·토큰 사용·상태별 스타일·동작
5. **인터랙션 & 플로우** — 로그인 가드, `/page` 입력 시 새 글 생성, 자동 저장, 삭제 확인, 아바타 → 마이 페이지 등 (README 흐름도와 코드 대조)
6. **접근성·반응형** — 코드에 존재하는 것만 (focus 표시, aria, 최소 크기 등). 없으면 "현재 미구현" 목록으로
7. **참조 이미지와의 차이점** — `03-reference-design.png` 대비 구현이 다른 부분 목록

## 4. 작성 후 자가 검증 (필수)

문서를 다 쓴 뒤 아래를 수행하고, 결과를 DESIGN.md 맨 끝 "검증" 섹션에 남겨줘.

1. `globals.css`의 커스텀 프로퍼티 개수를 세고, DESIGN.md에 기록된 토큰 개수와 대조 → 누락 0개 확인
2. 컴포넌트 파일 5개 각각에 대해 "이 파일의 스타일 정보 중 문서에 없는 것이 있는가" 재확인
3. 의도적으로 문서에서 제외한 항목이 있다면 이유와 함께 명시 (묵시적 생략 금지)

결과물은 프로젝트 루트에 `DESIGN.md`로 저장해줘.
