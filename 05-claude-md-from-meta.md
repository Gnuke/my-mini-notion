# 프롬프트: 디자인 작업 전 DESIGN.md 선행 참조 규칙을 CLAUDE.md에 추가

> 아래 전문을 Claude에게 그대로 전달한다.
> 목적: 이 프로젝트에서 수행하는 **모든 디자인 작업이 `DESIGN.md`를 먼저 읽고 시작**되도록,
> 프로젝트 루트 `CLAUDE.md`에 지시사항을 추가한다.

---

이 프로젝트의 루트 `CLAUDE.md`에 디자인 작업 규칙을 추가해줘.
핵심 요구는 하나야: **디자인과 관련된 작업이라면 어떤 것이든, 작업을 시작하기 전에 반드시 `DESIGN.md`를 먼저 읽고 진행할 것.**

## 1. 파일 처리 방식

- 프로젝트 루트에 `CLAUDE.md`가 **없으면 새로 생성**한다.
- 이미 **있으면 기존 내용은 그대로 두고**, `## 디자인 작업 규칙` 섹션만 추가한다 (기존 규칙과 중복되면 병합).

## 2. CLAUDE.md에 담아야 할 지시사항

추가하는 섹션은 아래 내용을 모두 포함해야 한다. 문구는 다듬어도 되지만 의미가 빠지면 안 된다.

1. **선행 참조 (가장 중요)** — 디자인 작업을 시작하기 전에 반드시 `DESIGN.md`를 Read 도구로 먼저 읽는다. 기억이나 요약에 의존하지 않는다.
2. **"디자인 작업"의 범위를 구체적으로 정의** — 아래 중 하나라도 해당하면 디자인 작업으로 간주한다:
   - 색상·타이포그래피·간격·radius·그림자·모션 등 스타일 값 변경 (`app/globals.css` 수정 포함)
   - 컴포넌트(`components/*`) 또는 화면(`app/**/page.tsx`, `layout.tsx`)의 UI 구조·인라인 스타일 변경
   - 새 컴포넌트·화면·상태(hover/active/focus/빈 상태 등) 추가
   - UI 문자열(버튼 라벨, placeholder, 안내 문구) 변경
3. **DESIGN.md를 단일 디자인 명세로 취급** — 색상·크기 등 값이 필요하면 임의로 정하지 말고 DESIGN.md의 토큰과 리터럴 값을 따른다. DESIGN.md에 "미구현"/"미정"으로 표기된 항목은 지어내지 말고 사용자에게 확인한다.
4. **불일치 처리** — 작업 중 코드와 DESIGN.md가 다른 부분을 발견하면 임의로 한쪽을 고치지 말고, 차이를 보고한 뒤 어느 쪽이 정답인지 확인받는다.
5. **문서 동기화** — 디자인을 변경하는 작업을 완료했다면, DESIGN.md의 해당 섹션도 같은 커밋 단위로 갱신해서 문서와 코드가 어긋나지 않게 한다.

## 3. 작성 스타일 요구

- 지시문은 **명령형·단문**으로 쓴다 ("~한다", "~하지 않는다"). 배경 설명은 최소화.
- Claude가 매 세션 자동으로 읽는 파일이므로, 섹션은 짧고 스캔 가능하게 유지한다 (목록 위주, 표 불필요).
- `DESIGN.md` 경로는 프로젝트 루트 기준 상대 경로로 표기한다.

## 4. 작업 후 확인 (필수)

1. 최종 `CLAUDE.md`에 추가된 섹션 전문을 보여줘.
2. 규칙이 실제로 동작하는지 자가 점검: "PostList의 선택 행 배경색을 바꿔줘" 같은 가상의 디자인 요청이 들어왔을 때, 추가한 규칙만 보고 ① DESIGN.md를 먼저 읽게 되는지 ② 토큰(`--tile-blue` 등)을 따르게 되는지 시나리오로 설명해줘.
