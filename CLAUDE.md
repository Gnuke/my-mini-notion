# CLAUDE.md

Nook — 미니 노션 (Next.js App Router, UI 전체 한국어).

## 디자인 작업 규칙

### 선행 참조 (가장 중요)

- 디자인 작업을 시작하기 전에 반드시 `DESIGN.md`를 Read 도구로 먼저 읽는다.
- 기억이나 이전 대화의 요약에 의존하지 않는다. 매번 파일을 직접 읽는다.

### "디자인 작업"의 범위

아래 중 하나라도 해당하면 디자인 작업으로 간주한다:

- 색상·타이포그래피·간격·radius·그림자·모션 등 스타일 값 변경 (`app/globals.css` 수정 포함)
- 컴포넌트(`components/*`) 또는 화면(`app/**/page.tsx`, `layout.tsx`)의 UI 구조·인라인 스타일 변경
- 새 컴포넌트·화면·상태(hover/active/focus/빈 상태 등) 추가
- UI 문자열(버튼 라벨, placeholder, 안내 문구) 변경

### DESIGN.md는 단일 디자인 명세

- 색상·크기 등 값이 필요하면 임의로 정하지 않는다. `DESIGN.md`의 토큰과 리터럴 값을 따른다.
- `DESIGN.md`에 "미구현"/"미정"으로 표기된 항목은 지어내지 않는다. 사용자에게 확인한다.

### 불일치 처리

- 작업 중 코드와 `DESIGN.md`가 다른 부분을 발견하면 임의로 한쪽을 고치지 않는다.
- 차이를 보고한 뒤 어느 쪽이 정답인지 확인받고 진행한다.

### 문서 동기화

- 디자인을 변경했으면 `DESIGN.md`의 해당 섹션도 같은 커밋 단위로 갱신한다.
- 문서와 코드가 어긋난 상태로 작업을 끝내지 않는다.
